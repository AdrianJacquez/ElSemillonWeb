---
// Importa el archivo JSON
import semillas from './EmpSemillas.json';
---

<section id="Semillas" class="pt-16 pb-10 bg-white text-gray-800">
  <div class="container mx-auto px-6  lg:px-12">
      <!-- Título de la sección -->
      <h2 class="text-4xl font-bold text-green-700 text-center mb-12 pt-6">Semillas</h2>

      <!-- Introducción -->
      <div class="mb-12 text-center">
          <p class="text-lg leading-relaxed max-w-3xl mx-auto text-justify">
            En El Semillón, sabemos que el éxito de una cosecha comienza con una buena semilla. Por eso, te ofrecemos una selección de semillas de alta calidad que garantizan un crecimiento robusto y una producción abundante. Ya seas un agricultor experimentado o un jardinero apasionado, estas semillas están diseñadas para satisfacer todas tus necesidades agrícolas.
          </p>
      </div>

      <!-- Especialidades de la Empresa -->
      <div class="flex flex-wrap justify-center">
        <!-- Germinado de Plantas -->
        {semillas.map(semilla => (
            <div class="flex justify-center items-center w-60 h-80 bg-green-50 mx-6 my-4 rounded-3xl text-green-800 p-4 shadow-lg shadow-black hover:shadow-lg hover:shadow-green-800 transition-shadow ">
                <div class="flex flex-col h-full justify-around items-start ">
<img src={semilla.imagen} alt={semilla.nombre} class="w-52 h-40 rounded-2xl mix-blend-multiply"/>

                    <div>
                        <p class="font-extrabold">{semilla.nombre}</p>
                        <p>{semilla.descripcion}</p>
                    </div>
                </div>
            </div>
        ))}
    </div>
    
    
  </div>
</section>
